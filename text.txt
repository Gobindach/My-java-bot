operator:
Operator is process which involbes to operate the function by the help of its operands .

int add(int x, int y)
{
    int z=x+y;
    return z;
}
unary +a,++a,--a,>>a,<<a
binary a+b,a*b
ternary (a:b) ? a